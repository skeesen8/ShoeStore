const allshoes = () => {
    return(
        <div>
            <h1> see all Shoes here</h1>
            {/* <ul>
                {shoes.map(shoe => (
                    <li key={shoe.id}>id ={shoe.id} name={shoe.name} price ={shoe.price}</li>
                ))}
            </ul> */}
        </div>
    )
}

export default allshoes;